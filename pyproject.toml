[project]
name = "wikipedia-interest-skill"
version = "0.1.0"
description = "Agent Skill for analyzing Wikipedia pageview trends"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pandas>=2.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]